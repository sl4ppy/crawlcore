import { create } from 'zustand';

// Meta-progression store for managing achievements, unlocks, and persistent progress
export interface MetaState {
  // TODO: Implement meta state interface
}

export const useMetaStore = create<MetaState>((_set, _get) => ({
  // TODO: Implement meta-progression state management
})); 